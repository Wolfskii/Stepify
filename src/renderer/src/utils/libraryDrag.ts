/** Drag-to-assign tracks onto sidebar dances (JSON array of track ids). */
export const TRACK_ASSIGN_DRAG_MIME = 'application/x-stepify-tracks'

/** Drag handle → reorder within the visible track list (JSON `{ fromIndex: number }`). */
export const TRACK_REORDER_DRAG_MIME = 'application/x-stepify-reorder'
